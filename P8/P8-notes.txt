🧩 PRACTICAL 8 — 3Switch-4PC-VLAN-Configuration.pkt
🔹 Objective:

Setup VLANs in a 3-switch network (each switch has 4 PCs).

⚙️ Concept:

Demonstrate VLAN communication through a main switch.

Each switch connected to the main switch through trunk ports.

⚙️ Commands:
Switch(config)# vlan 2
Switch(config)# vlan 3
Switch(config)# interface range fa0/1-4
Switch(config-if)# switchport access vlan 2
Switch(config-if)# exit
Switch(config)# interface fa0/24
Switch(config-if)# switchport mode trunk